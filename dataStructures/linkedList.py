class Node():
    def __init__(self,value,pointer):
        self.value = value 
        self.pointer = pointer

class LinkedList():
    def __init__(self):
        self.link = []
        self.headPointer = 0
        self.freePointer = 0
    def add(value):
        #add to the start
        #add in a specific position
        #add to the end
        link.append(Node(value,freePointer))
    def

linked = LinkedList()
linked.add(141)